name: Build Web App IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Dependencies
        run: |
          cd MelodyTrainer  # 切换到项目目录
          npm install

      - name: Build Web Assets
        run: |
          cd MelodyTrainer  # 切换到项目目录
          npm run build

      - name: Add iOS Platform
        run: |
          cd MelodyTrainer  # 切换到项目目录
          npx cap add ios

      - name: Build IPA (No Signing)
        run: |
          cd MelodyTrainer  # 切换到项目目录
          npx cap sync
          cd ios
          xcodebuild -workspace App/App.xcworkspace \
            -scheme App \
            -sdk iphoneos \
            -configuration Release \
            -archivePath $PWD/build/App.xcarchive \
            archive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_IDENTITY="" \
            AD_HOC_CODE_SIGNING_ALLOWED=YES

          # 手动打包成 IPA
          cd build/App.xcarchive/Products/Applications
          mkdir Payload
          mv App.app Payload/
          zip -r MelodyTrainer.ipa Payload

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-ipa
          path: MelodyTrainer/ios/build/App.xcarchive/Products/Applications/MelodyTrainer.ipa  # 修正 IPA 路径
