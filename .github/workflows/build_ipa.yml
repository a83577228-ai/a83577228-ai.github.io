name: Build iOS IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Dependencies
        run: |
          cd melodytraining  # 进入项目根目录
          npm install

      - name: Add iOS Platform
        run: |
          cd melodytraining  # 进入项目根目录
          npx cap add ios

      - name: Sync Project
        run: |
          cd melodytraining  # 进入项目根目录
          npx cap sync

      - name: Build Unsigned Archive
        run: |
          cd melodytraining/ios  # 进入iOS项目目录（带melodytraining前缀）
          xcodebuild -workspace App/App.xcworkspace \
            -scheme App \
            -sdk iphoneos \
            -configuration Release \
            -archivePath $PWD/build/App.xcarchive \
            archive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            ONLY_ACTIVE_ARCH=NO

      - name: Create Payload for AltStore
        run: |
          # 路径包含melodytraining前缀
          cd melodytraining/ios/build/App.xcarchive/Products/Applications
          mkdir Payload
          mv App.app Payload/
          zip -r MelodyTrainer.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: MelodyTrainer-IPA
          # 路径包含melodytraining前缀
          path: melodytraining/ios/build/App.xcarchive/Products/Applications/MelodyTrainer.ipa
