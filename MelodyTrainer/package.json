{
  "name": "melody-trainer",
  "version": "1.0.0",
  "description": "Melody Contour Training App",
  "scripts": {
    "build": "mkdir -p www && echo 'Building web assets' && cp index.html www/index.html"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }

}

